import React, {PureComponent} from 'react'
import Alert from '../../components/Alert'

class IndexSettings extends PureComponent {
  render () {
    return (
      <Alert info='Ajustes...' />
    )
  }
}

export default IndexSettings